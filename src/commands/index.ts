import * as ping from "./ping";
import * as setup from "./setup";
import * as openticket from "./openTicket";
import * as closeticket from "./closeTicket";

export const commands = {
  ping,
  setup,
  openticket,
  closeticket,
};
